# --- runtime ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
pydantic==2.10.4
pydantic-settings==2.7.0
PyJWT==2.10.1
passlib==1.7.4
# passlib 1.7.4 reads bcrypt.__about__, removed in bcrypt>=4.1 - pin to a compatible release
bcrypt==4.0.1

# --- lint / type checking ---
ruff==0.8.4
mypy==1.14.0

# --- tests (kept in the image so `docker compose run --rm api pytest` needs zero setup) ---
pytest==8.3.4
pytest-asyncio==0.25.0
pytest-cov==6.0.0
httpx==0.28.1
